import React from "react";

const Card = ({ label, color, horizontalAlign, verticalAlign }) => (
  <div /*style={{ background: { color }  }}*/>
    <h3>{label}</h3>
  </div>
);

export default Card;
